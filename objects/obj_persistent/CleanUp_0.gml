/// @description Clean up the particle system

part_type_destroy(glitter_collect);
part_type_destroy(glitter_gui);

part_type_destroy(glitter_collect_flash);
part_type_destroy(glitter_gui_flash);